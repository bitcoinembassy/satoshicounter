Template.navbar.helpers({
  mainCurrency: function() {
    // var mainCurrency = Currencies.findOne({name: orion.dictionary.get('company.mainCurrency', 'BTC')});
    // console.log(mainCurrency)
    return 'bitcoins';
  }
});
